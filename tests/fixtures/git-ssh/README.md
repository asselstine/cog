# Redacted Git packet fixtures

These hexadecimal pkt-line transcripts contain no repository names,
credentials, object contents, or provider response bodies. They cover the
framing boundaries exercised by the SSH/smart-HTTP adapter. Pack payloads are
represented by a bounded marker because production pack bytes always stream
without interpretation.
