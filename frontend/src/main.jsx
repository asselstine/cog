import React,{useEffect,useState}from"react";
import{createRoot}from"react-dom/client";
import{Cable,Check,ChevronDown,KeyRound,LogOut,Moon,PlugZap,ShieldCheck,Sun,Trash2,Users}from"lucide-react";
import"./index.css";
const body=v=>new URLSearchParams(v);
async function submit(path,values){const r=await fetch(path,{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:body(values)});if(!r.ok)throw new Error(await r.text()||`Request failed (${r.status})`)}
function Shell({children,signedIn,signOut}){const[dark,setDark]=useState(()=>document.documentElement.classList.contains("dark"));return <main className="mx-auto min-h-screen max-w-6xl px-5 py-8"><header className="mb-8 flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-blue-500 text-white"><Cable size={21}/></div><div><b>COG</b><div className="text-xs text-zinc-500">Clanker Operations Gateway</div></div><div className="ml-auto flex items-center gap-2">{signedIn&&<button className="button-secondary h-9" onClick={signOut}><LogOut size={16}/> Sign out</button>}<button className="icon-button" onClick={()=>{document.documentElement.classList.toggle("dark",!dark);setDark(!dark)}}>{dark?<Sun size={17}/>:<Moon size={17}/>}</button></div></header>{children}</main>}
function Login({reload}){const[error,setError]=useState("");return <div className="grid min-h-[70vh] place-items-center"><section className="card w-full max-w-md p-8"><p className="eyebrow">Welcome back</p><h1 className="mt-3 text-3xl font-bold">Sign in to COG</h1><p className="mt-3 text-sm text-zinc-500">Manage identities, connections, agents, and permissions.</p><form className="mt-7 space-y-4" onSubmit={async e=>{e.preventDefault();try{await submit("/login",Object.fromEntries(new FormData(e.currentTarget)));reload()}catch(x){setError(x.message)}}}><label className="block text-sm">Email<input className="input mt-2" name="email" type="email" required/></label><label className="block text-sm">Password<input className="input mt-2" name="password" type="password" required/></label>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="button w-full">Sign in</button></form></section></div>}
function GitHubInstallationComplete(){const integration=new URLSearchParams(window.location.search).get("integration_id");return <div className="grid min-h-[70vh] place-items-center"><section className="card w-full max-w-xl p-8 text-center sm:p-10"><div className="mx-auto grid size-14 place-items-center rounded-full bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-300"><Check size={28} strokeWidth={2.5}/></div><p className="eyebrow mt-6">GitHub connected</p><h1 className="mt-3 text-3xl font-bold">GitHub App installed</h1><p className="mt-4 text-sm leading-6 text-zinc-500">The GitHub integration is ready. You can close this window and retry repository access.</p>{integration&&<div className="mt-6 rounded-xl border border-zinc-200 bg-zinc-50 p-3 text-xs text-zinc-500 dark:border-white/10 dark:bg-black/20">Integration <code>{integration}</code></div>}<button className="button-secondary mt-6" onClick={()=>window.close()}>Close window</button></section></div>}
function Permission({permission}){return <label className={`grid grid-cols-[auto_1fr_auto] items-start gap-3 rounded-xl border p-4 transition ${permission.tone==="new"?"border-blue-400/70 bg-blue-50/80 dark:bg-blue-500/10":permission.tone==="other"?"border-zinc-200 bg-white opacity-65 dark:border-white/10 dark:bg-black/20":"border-zinc-200 bg-white dark:border-white/10 dark:bg-black/20"}`}><input className="mt-1 size-4 accent-blue-500" type="checkbox" name={permission.field||undefined} value="on" defaultChecked={permission.checked} disabled={permission.disabled}/><span><strong className="block text-sm">{permission.label}</strong><span className="mt-1 block text-xs leading-5 text-zinc-500 dark:text-zinc-400">{permission.description}</span></span>{permission.badge&&<span className="rounded-full bg-blue-100 px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-blue-700 dark:bg-blue-500/15 dark:text-blue-200">{permission.badge}</span>}</label>}
function Consent(){const[data,setData]=useState();const[error,setError]=useState();const[identity,setIdentity]=useState();useEffect(()=>{fetch(`/api/oauth/consent${window.location.search}`).then(async response=>{const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.message||payload.error||`Unable to load authorization (${response.status})`);setData(payload);setIdentity(payload.fixedIdentity?.id||payload.identities[0]?.id||"")}).catch(x=>setError(x.message))},[]);if(error)return <div className="grid min-h-[70vh] place-items-center"><section className="card w-full max-w-xl p-8"><p className="eyebrow">Authorization paused</p><h1 className="mt-3 text-3xl font-bold">Unable to continue</h1><p className="mt-4 text-sm leading-6 text-zinc-500">{error}</p><a className="button-secondary mt-6" href="/login">Sign in to COG</a></section></div>;if(!data)return <div className="grid min-h-[70vh] place-items-center text-sm text-zinc-500">Loading authorization…</div>;const creating=!data.fixedIdentity&&identity==="";return <div className="grid min-h-[70vh] place-items-center py-8"><section className="card w-full max-w-2xl p-6 sm:p-9"><p className="eyebrow">Agent authorization</p><h1 className="mt-3 text-3xl font-bold">Approve access</h1><p className="mt-3 text-sm leading-6 text-zinc-500">Review access for this agent. Any grant change affects every agent connected to the selected identity.</p><div className="my-6 flex items-center gap-3 rounded-xl border border-zinc-200 bg-zinc-50 p-4 dark:border-white/10 dark:bg-black/20"><div className="grid size-11 shrink-0 place-items-center rounded-xl bg-blue-100 font-bold text-blue-600 dark:bg-blue-500/15 dark:text-blue-300">A</div><div className="min-w-0"><div className="font-semibold">{data.client.name}</div><div className="truncate text-xs text-zinc-500"><code>{data.client.id}</code> · Returns securely to {data.client.redirectHost}</div></div></div><form method="post" action="/api/oauth/consent"><input type="hidden" name="consent" value={data.consent}/><input type="hidden" name="csrf_token" value={data.csrfToken}/>{data.fixedIdentity?<><input type="hidden" name="identity_id" value={data.fixedIdentity.id}/><div className="mb-6 rounded-xl border border-zinc-200 bg-zinc-50 p-4 text-sm dark:border-white/10 dark:bg-black/20"><strong>Identity: {data.fixedIdentity.name}</strong><div className="mt-1 text-zinc-500">This existing agent cannot switch identities.</div></div></>:<fieldset className="mb-7"><legend className="eyebrow mb-2">Identity</legend><p className="mb-3 text-xs leading-5 text-zinc-500">Connections and permissions are shared by every agent in this identity.</p><div className="relative"><select className="input cursor-pointer appearance-none pr-10 font-semibold" name="identity_id" value={identity} onChange={event=>setIdentity(event.target.value)}>{data.identities.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}<option value="">New identity</option></select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-zinc-400" size={18}/></div>{creating&&<label className="mt-4 block text-sm font-medium">New identity name<input className="input mt-2" name="new_identity_name" maxLength="128" required autoFocus/></label>}</fieldset>}<fieldset><legend className="eyebrow mb-3">Permissions</legend><div className="space-y-6">{data.permissionGroups.map(group=><section key={group.title}><h2 className="mb-2 text-xs font-semibold uppercase tracking-wider text-zinc-500">{group.title}</h2><div className="space-y-2">{group.permissions.map(permission=><Permission key={permission.field||permission.scope||permission.label} permission={permission}/>)}</div></section>)}</div></fieldset><p className="mt-6 flex gap-2 text-xs leading-5 text-zinc-500"><ShieldCheck className="mt-0.5 shrink-0" size={15}/> You can revoke this agent from the COG dashboard at any time.</p><div className="mt-6 flex flex-col-reverse gap-3 sm:flex-row"><button className="button-secondary" name="decision" value="deny">Cancel</button><button className="button sm:ml-auto" name="decision" value="allow">Allow selected access</button></div></form></section></div>}
const short=id=>id?.slice(0,8)||"";
const Pills=({values=[]})=><div className="flex flex-wrap gap-1.5">{values.length?values.map(v=><span key={v} className="rounded-full bg-zinc-100 px-2 py-0.5 text-xs dark:bg-white/5">{v}</span>):<span className="text-xs text-zinc-400">none</span>}</div>;
function Dashboard({data,reload}){const[open,setOpen]=useState(new Set());const[error,setError]=useState("");async function act(path,v={}){try{await submit(path,{...v,csrf_token:data.csrf_token});reload()}catch(x){setError(x.message)}}function toggle(id){setOpen(s=>{const n=new Set(s);n.has(id)?n.delete(id):n.add(id);return n})}return <div className="space-y-5">{error&&<div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="flex items-end justify-between"><div><p className="eyebrow">Access profiles</p><h1 className="mt-2 text-3xl font-bold">Identities</h1><p className="mt-2 text-sm text-zinc-500">Each identity has its own provider persona, connections, agents, and shared permissions.</p></div><button className="button" onClick={()=>{const name=prompt("Identity name");if(name)act("/ui/identities",{name})}}>Create identity</button></div>{!data.identities?.length?<section className="card p-10 text-center"><Users className="mx-auto text-blue-500"/><h2 className="mt-4 font-semibold">Create your first identity</h2><p className="mt-2 text-sm text-zinc-500">Identities separate provider accounts and access policies.</p></section>:data.identities.map(identity=>{const expanded=open.has(identity.id);const tokens=data.tokens.filter(t=>identity.agents.some(a=>a.oauth_client_id===t.client_id));return <section className="card overflow-hidden" key={identity.id}><div className="flex items-center gap-3 border-b border-zinc-200 p-4 dark:border-white/10"><button className="icon-button" onClick={()=>toggle(identity.id)}><ChevronDown className={expanded?"rotate-180":""}/></button><div><h2 className="font-semibold">{identity.name}</h2><code className="text-[11px] text-zinc-400">{short(identity.id)}</code></div><div className="ml-auto text-xs text-zinc-500">{identity.connections.length} connections · {identity.agents.length} agents</div><button className="button-secondary" onClick={()=>{const name=prompt("Rename identity",identity.name);if(name)act(`/ui/identities/${identity.id}/rename`,{name})}}>Rename</button><button className="button-danger" onClick={()=>confirm(`Delete ${identity.name} and all of its connections, agents, credentials, and grants?`)&&act(`/ui/identities/${identity.id}/delete`)}><Trash2 size={15}/></button></div>{expanded&&<div className="grid gap-5 p-4 lg:grid-cols-3"><div><h3 className="mb-3 font-semibold">Connections</h3>{identity.connections.length?identity.connections.map(c=><div className="mb-2 rounded-xl border border-zinc-200 p-3 dark:border-white/10" key={c.id}><div className="font-medium">{c.display_name}</div><div className="text-xs text-zinc-500">{c.transport} · {c.oauth}</div>{(c.provider_name||c.provider_account)&&<div className="mt-1 text-xs text-zinc-400">{[c.provider_name,c.provider_account].filter(Boolean).join(" · ")}</div>}<Pills values={c.oauth_scopes}/><div className="mt-2 flex gap-2"><button className="button-secondary" onClick={()=>confirm("Disconnect credentials but preserve this connection?")&&act(`/ui/integrations/${c.id}/disconnect`)}><PlugZap size={14}/></button><button className="button-danger" onClick={()=>confirm("Delete this connection and every descendant?")&&act(`/ui/integrations/${c.id}/delete`)}><Trash2 size={14}/></button></div></div>):<p className="text-sm text-zinc-400">No connections.</p>}</div><div><h3 className="mb-3 font-semibold">Agents</h3>{identity.agents.length?identity.agents.map(a=><div className="mb-2 rounded-xl border border-zinc-200 p-3 dark:border-white/10" key={a.id}><div className="font-medium">{a.display_name}</div><code className="text-[11px] text-zinc-400">agent {short(a.id)} · client {short(a.oauth_client_id)}</code>{a.registered_name!==a.display_name&&<div className="text-xs text-zinc-500">Registered as {a.registered_name}</div>}<div className="mt-2 flex items-center justify-between text-xs text-zinc-500"><span><KeyRound className="inline" size={13}/> {tokens.filter(t=>t.client_id===a.oauth_client_id).length} credentials</span><button className="button-secondary" onClick={()=>{const name=prompt("Rename agent",a.display_name);if(name)act(`/ui/agents/${a.id}/rename`,{name})}}>Rename</button></div></div>):<p className="text-sm text-zinc-400">No authorized agents.</p>}</div><div><h3 className="mb-3 font-semibold">Permissions</h3><p className="mb-3 text-xs text-amber-600">Changes affect every agent in this identity.</p><Pills values={identity.grants}/></div></div>}</section>})}<footer className="flex items-center gap-2 text-xs text-zinc-500"><ShieldCheck size={14}/> Secrets and credential values are never displayed.</footer></div>}
function App(){const consent=window.location.pathname==="/oauth/authorize";const githubComplete=window.location.pathname==="/github/app/installation/complete";const standalone=consent||githubComplete;const[data,setData]=useState();const[error,setError]=useState("");async function load(){try{const r=await fetch("/api/ui");if(!r.ok)throw new Error("Unable to load COG");setData(await r.json())}catch(x){setError(x.message)}}useEffect(()=>{if(!standalone)load()},[standalone]);return <Shell signedIn={!standalone&&data?.mode==="admin"} signOut={async()=>{await submit("/logout",{csrf_token:data.csrf_token});load()}}>{consent?<Consent/>:githubComplete?<GitHubInstallationComplete/>:error?<div>{error}</div>:!data?<div>Loading…</div>:data.mode==="admin"?<Dashboard data={data} reload={load}/>:<Login reload={load}/>}</Shell>}
createRoot(document.getElementById("root")).render(<React.StrictMode><App/></React.StrictMode>);
