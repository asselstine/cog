# TODO

There are no active repository TODOs.

The completed file-structure cleanup and official Rust MCP SDK flag-day rewrite
are recorded in `docs/history/file-structure-cleanup.md` and
`docs/history/rmcp-rewrite.md`. Deployment-backed MCP Inspector and official
conformance runs remain release validation, as do the explicitly documented
transitive dependency advisories; they do not require parallel protocol code or
unfinished implementation in this tree.
